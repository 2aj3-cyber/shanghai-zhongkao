# 上海中考升学路径全分析 2026版

单文件静态工具（纯前端，零依赖，可离线打开）。

## 本地使用
直接双击 `index.html` 即可，无需任何服务器。

## 部署
本目录已配置为 GitHub Pages 源（`main` 分支根目录，` .nojekyll` 已就位）。
推送后到仓库 Settings → Pages → Source 选 `main` / `(root)` 即可上线，24/7 永久可访问。
